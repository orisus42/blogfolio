{
  "name": "blogfolio",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve dist"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "marked-katex-extension": "^5.0.0",
    "katex": "^0.16.10"
  }
}
